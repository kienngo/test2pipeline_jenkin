pipeline {
    agent any

    environment {
        dotnet = 'C:\\Program Files\\dotnet\\dotnet.exe'
    }

    stages {
        stage('Build') { 
            steps {
                bat 'dotnet build ProjectSample\\ProjectSample.sln --configuration Release'
            }
        }

        stage('Test') {
            steps {
                bat 'dotnet test ProjectSample\\ProjectSample.csproj'
            }
        }

        stage('Release') {
            steps {
                bat 'dotnet build ProjectSample\\ProjectSample.sln /p:PublishProfile=" ProjectSample\\Properties\\PublishProfiles\\FolderProfile.pubxml" /p:Platform="Any CPU" /p:DeployOnBuild=true /m'
            }
        }
    }
}
